# ARAMBOIA landing page

Responsive single-page website for `aramboia.com.br`.

## Local preview

Run any static web server in this directory, for example:

```sh
python3 -m http.server 8080
```

Then open `http://localhost:8080`.

## Hosting

The site is designed for GitHub Pages and includes the primary custom domain in `CNAME`.
